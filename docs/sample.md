# 可直接渲染的 Markdown 文件

这个文件位于 `docs/sample.md`。

访问 `/reader/?file=docs/sample.md` 时，站点会在浏览器中读取这个 `.md` 文件并渲染为文章样式。

## 示例

- 支持标题、列表、表格和代码块
- 适合放项目文档、读书笔记、公开说明
- 不需要为每个文档单独创建页面

```bash
git add .
git commit -m "Create personal blog"
git push
```
